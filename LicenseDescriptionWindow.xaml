<Window x:Class="IMS.LicenseDescriptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="License Details"
        Width="500" Height="310"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        FontFamily="Segoe UI">
  <!-- Ensure Segoe UI is used for a modern look -->

  <Window.Resources>
    <!-- Modern Button Style for Close Button -->
    <Style x:Key="CloseButtonStyle" TargetType="Button">
      <Setter Property="Width" Value="35"/>
      <Setter Property="Height" Value="35"/>
      <Setter Property="Margin" Value="0,0,2,0"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="#fff"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="HorizontalContentAlignment" Value="Center"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Button}">
            <Border Background="{TemplateBinding Background}" CornerRadius="0,0,0,0">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FFDDDD"/>
                <Setter Property="Foreground" Value="#E74C3C"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#E74C3C"/>
                <Setter Property="Foreground" Value="White"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Accent Button Style for OK button -->
    <Style x:Key="AccentButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="#007ACC"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="FontSize" Value="16"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Padding" Value="15,8"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Button}">
            <Border x:Name="ButtonBorder" CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
              <ContentPresenter x:Name="Content"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ButtonBorder" Property="Background">
                  <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                      <GradientStop Color="#007ACC" Offset="0"/>
                      <GradientStop Color="#3498DB" Offset="1"/>
                    </LinearGradientBrush>
                  </Setter.Value>
                </Setter>
                <Setter TargetName="ButtonBorder" Property="Effect">
                  <Setter.Value>
                    <DropShadowEffect BlurRadius="15" Opacity="0.4" Color="#007ACC" ShadowDepth="4"/>
                  </Setter.Value>
                </Setter>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ButtonBorder" Property="Background" Value="#005C99"/>
                <Setter TargetName="ButtonBorder" Property="Effect">
                  <Setter.Value>
                    <DropShadowEffect BlurRadius="5" Opacity="0.2" Color="#007ACC" ShadowDepth="2"/>
                  </Setter.Value>
                </Setter>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="ButtonBorder" Property="Background" Value="#CCCCCC"/>
                <Setter Property="Foreground" Value="#666666"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Style for the detail TextBlocks -->
    <Style x:Key="DetailLabelStyle" TargetType="TextBlock">
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="FontSize" Value="15"/>
      <Setter Property="Foreground" Value="#555555"/>
      <Setter Property="Margin" Value="0,0,0,8"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="DetailValueStyle" TargetType="TextBlock">
      <Setter Property="FontSize" Value="15"/>
      <Setter Property="Foreground" Value="#2D2D2D"/>
      <Setter Property="Margin" Value="0,0,0,8"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

  </Window.Resources>

  <Border Background="White" CornerRadius="0" BorderBrush="Transparent" BorderThickness="1"
            Padding="0" Margin="0">
    <Border.Effect>
      <DropShadowEffect BlurRadius="25" Opacity="0.8" ShadowDepth="8" Direction="270" Color="#000000"/>
    </Border.Effect>

    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <!-- Custom Title Bar -->
        <RowDefinition Height="*"/>
        <!-- Content -->
      </Grid.RowDefinitions>

      <!-- Custom Title Bar -->
      <Border Grid.Row="0" Background="#FF333333" CornerRadius="0"
                    MouseLeftButtonDown="Window_MouseLeftButtonDown" Height="40">
        <Grid>
          <TextBlock Text="IMS License Details"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#fff"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left" Margin="20,0,0,0"/>
          <Button Content="&#xE8BB;"
                            Style="{StaticResource CloseButtonStyle}"
                            HorizontalAlignment="Right"
                            FontFamily="Segoe MDL2 Assets"
                            Click="OkButton_Click"/>
          <!-- Use the same click handler as OK -->
        </Grid>
      </Border>

      <!-- Main Content Area -->
      <StackPanel Grid.Row="1" Margin="25">
        <Grid Margin="0,0,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" SharedSizeGroup="LabelColumn"/>
            <ColumnDefinition Width="Auto" SharedSizeGroup="ValueColumn"/>
            <ColumnDefinition Width="*"/>
            <!-- To push columns to left -->
          </Grid.ColumnDefinitions>

          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>

          <!-- Using the new styles and SharedSizeGroup for alignment -->
          <TextBlock Grid.Row="0" Grid.Column="0" Text="Client Name:" Style="{StaticResource DetailLabelStyle}" Margin="0,0,15,8"/>
          <TextBlock Grid.Row="0" Grid.Column="1" Text="HIJAZI_AND_GHOSHEH" Style="{StaticResource DetailValueStyle}"/>

          <TextBlock Grid.Row="1" Grid.Column="0" Text="License Type:" Style="{StaticResource DetailLabelStyle}" Margin="0,0,15,8"/>
          <TextBlock Grid.Row="1" Grid.Column="1" Text="Enterprise" Style="{StaticResource DetailValueStyle}"/>

          <TextBlock Grid.Row="2" Grid.Column="0" Text="Named Users:" Style="{StaticResource DetailLabelStyle}" Margin="0,0,15,8"/>
          <TextBlock Grid.Row="2" Grid.Column="1" Text="Open License" Style="{StaticResource DetailValueStyle}"/>

          <TextBlock Grid.Row="3" Grid.Column="0" Text="Concurrent Users:" Style="{StaticResource DetailLabelStyle}" Margin="0,0,15,8"/>
          <TextBlock Grid.Row="3" Grid.Column="1" Text="Open License" Style="{StaticResource DetailValueStyle}"/>

          <TextBlock Grid.Row="4" Grid.Column="0" Text="Current Reserved Users:" Style="{StaticResource DetailLabelStyle}" Margin="0,0,15,8"/>
          <TextBlock Grid.Row="4" Grid.Column="1" Text="339" Style="{StaticResource DetailValueStyle}"/>

          <TextBlock Grid.Row="5" Grid.Column="0" Text="Current Users Imported:" Style="{StaticResource DetailLabelStyle}" Margin="0,0,15,8"/>
          <TextBlock Grid.Row="5" Grid.Column="1" Text="339" Style="{StaticResource DetailValueStyle}"/>
        </Grid>

        <Button Content="OK"
                        Width="150"
                        Height="40"
                        HorizontalAlignment="Right"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="OkButton_Click"
                        Margin="0,5,0,0"/>
        <!-- Adjusted margin for spacing -->
      </StackPanel>
    </Grid>
  </Border>
</Window>