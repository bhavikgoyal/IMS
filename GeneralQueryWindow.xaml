<Window x:Class="IMS.GeneralQueryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IMS"
        mc:Ignorable="d"
        Title="GeneralQueryWindow"   Height="205"
        Width="500" >
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- VB6 jaisi line -->
    <TextBlock Grid.Row="0"
                   Margin="0,0,0,5"
                   Text="Write down a Proper SQL Query"
                   FontSize="14"
                   VerticalAlignment="Center"/>

    <!-- Query textbox -->
    <TextBox x:Name="txtSql"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto" Margin="0,0,0,72"/>

    <!-- OK / Cancel -->
    <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,8,0,0">
      <Button Width="80" Margin="0,0,5,0" IsDefault="True"
                    Content="OK"
                    Click="Ok_Click"/>
      <Button Width="80" IsCancel="True"
                    Content="Cancel"
                    Click="Cancel_Click" RenderTransformOrigin="0.5,0.5">
        <Button.RenderTransform>
          <TransformGroup>
            <ScaleTransform/>
            <SkewTransform/>
            <RotateTransform Angle="1.259"/>
            <TranslateTransform/>
          </TransformGroup>
        </Button.RenderTransform>
      </Button>
    </StackPanel>
  </Grid>
</Window>
